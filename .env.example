# V1 AI Guardrails — Environment (claude_enterprise OAuth mode)
# No API key needed — your enterprise OAuth token is managed by Claude Code.
# Copy this to .env — never commit .env to git.

# Optional: protect the proxy with a local key if you later share it with teammates
# LITELLM_MASTER_KEY=v1-guardrails-local
